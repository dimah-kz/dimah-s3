# examples/with-vite

Same shape as [`templates/vite`](../../templates/vite) — monorepo demo only (`workspace:*` `@dimah-s3/*`). No database or auth.

User-facing starter: `npx @dimah-s3/cli@latest create my-app --template vite` (from `templates/vite`), not this folder.
