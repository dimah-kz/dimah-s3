# examples/with-hono

Same shape as [`templates/hono`](../../templates/hono) — monorepo demo only (`workspace:*` `@dimah-s3/*`). No database or auth.

User-facing starter: `npx @dimah-s3/cli@latest create my-app --template hono` (from `templates/hono`), not this folder.
