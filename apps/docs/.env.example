# -----------------------------------------------------------------------------
# Docs site — environment variables
# -----------------------------------------------------------------------------
#
# Production canonical is https://s3.dimah.dev when VERCEL_ENV=production.
# Set NEXT_PUBLIC_SITE_URL only to override (local tunnel, etc.).
#
# Dashboard: Project → Settings → Environment Variables
#   NEXT_PUBLIC_SITE_URL → optional override → https://s3.dimah.dev
#
# Auto-injected by Vercel (do not copy into .env.local):
#   VERCEL_URL
#   VERCEL_ENV                    production | preview | development
#
# Local dev (optional):
# NEXT_PUBLIC_SITE_URL=http://localhost:3000
