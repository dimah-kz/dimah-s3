---
title: Upload Button
description: File picker button for single- or multi-file uploads with inline status.
---

import { UploadButtonDemo } from "@/components/demos/upload-button-demo";

`UploadButton` opens the native file picker and uploads through
[`useUpload`](/docs/react/hooks/upload) / [`useMultiUpload`](/docs/react/hooks/upload#usemultiupload).

## Preview

<DemoPreview name="upload-button-demo.tsx">
  <UploadButtonDemo />
</DemoPreview>

## Install

One-time styles and toaster: [Setup](/docs/react/ui/setup).

<Tabs items={["npm package", "shadcn registry"]}>

<Tab value="npm package">

```tsx
import { UploadButton } from "@dimah-s3/ui";
```

</Tab>

<Tab value="shadcn registry">

```npm
npx shadcn@latest add @dimah-s3/upload-button
```

</Tab>

</Tabs>

## Usage

```tsx
<UploadButton
  objectKey={(f) => `uploads/${f.name}`}
  maxFiles={5}
  variant="secondary"
  size="sm"
  attachmentSize="xs"
/>
```

Inline status uses [`Attachment`](/docs/react/ui/components/attachment) rows. Control it with
`status`:

| Value                 | Behavior                    |
| --------------------- | --------------------------- |
| `true` (default)      | Render below the button     |
| `false`               | Hide status                 |
| `(node) => ReactNode` | Wrap or relocate the status |

`attachmentSize` / `attachmentOrientation` set row density without changing
Button `size`. Toasts are independent via the `toast` prop.

<Callout type="warn">
  `accept` and `maxFileSize` are enforced **client-side only**. Mirror the same
  limits in `upload.presignGuard` on the server.
</Callout>

## Props

<AutoTypeTable
  path="packages/ui/src/components/dimah-s3/upload/upload-button.tsx"
  name="UploadButtonProps"
/>

## Headless

Custom trigger: see [Custom UI](/docs/react/ui/customization/custom-ui)
([`useUpload`](/docs/react/hooks/upload#useupload) +
[`FileAttachment`](/docs/react/ui/components/attachment) / `UploadStatus`).
