---
title: Upload Dropzone
description: Drag-and-drop upload area for single- or multi-file uploads.
---

import { UploadDropzoneDemo } from "@/components/demos/upload-dropzone-demo";

`UploadDropzone` is a drop target with the same upload options as
[`UploadButton`](/docs/react/ui/upload-button). Set `maxFiles` above `1` for
multi-file uploads.

## Preview

<DemoPreview name="upload-dropzone-demo.tsx">
  <UploadDropzoneDemo />
</DemoPreview>

## Install

<Tabs items={["npm package", "shadcn registry"]}>

<Tab value="npm package">
  <include>../../../_includes/ui/index.mdx#npm-package</include>
</Tab>

<Tab value="shadcn registry">
<include>../../../_includes/ui/index.mdx#shadcn-registry-setup</include>

```npm
npx shadcn@latest add @dimah-s3/upload-dropzone
```

</Tab>

</Tabs>

## Usage

```tsx
<UploadDropzone
  objectKey={(f) => `uploads/${f.name}`}
  maxFiles={5}
  attachmentSize="xs"
  status={(node) => (
    <div className="w-full border-t border-dashed p-3">{node}</div>
  )}
>
  <div className="flex flex-1 items-center justify-center p-8">
    Drop files here
  </div>
</UploadDropzone>
```

`children` replaces the idle chrome only. Status follows the same `status` prop
as `UploadButton` (`true` | `false` | render function). To own the entire
interior, set `status={false}` and render `UploadStatus` yourself.

<Callout type="warn">
  `accept` and `maxFileSize` are enforced **client-side only**. Mirror the same
  limits in `upload.presignGuard` on the server.
</Callout>

## Props

<AutoTypeTable
  path="packages/ui/src/components/dimah-s3/upload/upload-dropzone.tsx"
  name="UploadDropzoneProps"
/>

## Headless

Use [`useUpload`](/docs/react/hooks/upload#useupload) or
[`useMultiUpload`](/docs/react/hooks/upload#usemultiupload) for a custom drop
surface.
