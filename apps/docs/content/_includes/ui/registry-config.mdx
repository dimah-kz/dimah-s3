```json title="components.json"
{
  "registries": {
    "@dimah-s3": "https://dimah-s3.vercel.app/r/{name}.json"
  }
}
```

Without a `registries` entry, the same items install from GitHub:

```bash
pnpm dlx shadcn@latest add dimah-kz/dimah-s3/upload-dropzone
```
