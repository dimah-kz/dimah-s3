```npm
npm i @dimah-s3/ui shadcn
```

<include meta='title="app/globals.css"'>./globals.css</include>

`@dimah-s3/ui/styles.css` registers Tailwind sources and maps `--color-dimah-s3-*`
to your [shadcn theme](https://ui.shadcn.com/docs/theming) variables. Keep the
usual shadcn `--primary` / `--muted` / … values in `globals.css`.
