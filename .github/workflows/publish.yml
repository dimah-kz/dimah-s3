name: Publish

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    name: Version & Publish
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    permissions:
      contents: write
      id-token: write
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
          # Needed so NODE_AUTH_TOKEN is written for classic-token fallback.
          # Prefer OIDC trusted publishers when configured; token covers gaps
          # (e.g. a new package without a Trusted Publisher yet).
          registry-url: https://registry.npmjs.org
          scope: "@dimah-s3"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Versions when pending `.tegami/*.md` changelogs exist (opens a Version
      # Packages PR). Otherwise publishes from `.tegami/publish-lock.yaml`.
      # Package builds run in `willPublish` (see scripts/tegami.mts).
      # Keep `id-token: write` for npm OIDC trusted publishing. NODE_AUTH_TOKEN
      # is a fallback when a package has no Trusted Publisher on npmjs.com yet
      # (OIDC token exchange returns 404 → classic token publish).
      - name: Version & publish packages
        run: pnpm tegami ci
        env:
          GITHUB_TOKEN: ${{ github.token }}
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
