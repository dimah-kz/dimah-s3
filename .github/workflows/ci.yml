name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Lint & Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Format check
        run: pnpm format:check

  packages:
    name: Build, Test & Types
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Build packages
        run: pnpm turbo run build --filter="./packages/*"

      - name: Test packages
        run: pnpm turbo run test --filter="./packages/*"

      - name: Type check packages
        run: pnpm turbo run check-types --filter="./packages/*"

  registry:
    name: Validate Registry
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            registry:
              - 'registry/**'
              - 'packages/ui/**'
              - 'packages/react/**'
              - 'packages/core/**'

      - uses: pnpm/action-setup@v6
        if: steps.filter.outputs.registry == 'true'

      - uses: actions/setup-node@v6
        if: steps.filter.outputs.registry == 'true'
        with:
          node-version: 24
          cache: pnpm

      - if: steps.filter.outputs.registry == 'true'
        run: pnpm install --frozen-lockfile

      # Synced registry sources import @dimah-s3/{core,react}; types come from dist/.
      - name: Build package deps
        if: steps.filter.outputs.registry == 'true'
        run: pnpm turbo run build --filter=@dimah-s3/react

      - name: Sync registry sources
        if: steps.filter.outputs.registry == 'true'
        run: pnpm --filter @dimah-s3/ui sync-registry

      - name: Validate registry
        if: steps.filter.outputs.registry == 'true'
        run: pnpm --filter @dimah-s3/registry validate

      - name: Type check registry items
        if: steps.filter.outputs.registry == 'true'
        run: pnpm --filter @dimah-s3/registry check-types

  apps:
    name: Build Apps
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            apps:
              - 'apps/**'
              - 'packages/**'
              - 'registry/**'

      - uses: pnpm/action-setup@v6
        if: steps.filter.outputs.apps == 'true'

      - uses: actions/setup-node@v6
        if: steps.filter.outputs.apps == 'true'
        with:
          node-version: 24
          cache: pnpm

      - if: steps.filter.outputs.apps == 'true'
        run: pnpm install --frozen-lockfile

      - name: Build apps
        if: steps.filter.outputs.apps == 'true'
        run: pnpm turbo run build --filter="./apps/*"

      - name: Type check apps
        if: steps.filter.outputs.apps == 'true'
        run: pnpm turbo run check-types --filter="./apps/*"
