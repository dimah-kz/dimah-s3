---
packages:
  group:dimah-s3: patch
---

### Fix attachment title shimmer in dark mode

Uploading and processing titles use `shimmer-color-dimah-s3-muted-foreground` so the highlight stays visible on light text and follows the host theme.
