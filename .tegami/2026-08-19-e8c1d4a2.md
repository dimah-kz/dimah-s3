---
packages:
  group:dimah-s3: patch
---

### Fix invalid `*/*` dropzone accept

Extension-only `accept` entries such as `.pdf` now map to a real MIME type. The file picker no longer warns that `*/*` is not a valid MIME type.
